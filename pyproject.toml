[project]
name = "yolo-images"
version = "0.1.0"
description = "EXIF Location Tagger for Immich"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
    "httpx>=0.28",
    "python-dotenv>=1.1",
]

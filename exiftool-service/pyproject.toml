[project]
name = "exiftool-service"
version = "0.1.0"
description = "Exiftool GPS writer microservice"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
]

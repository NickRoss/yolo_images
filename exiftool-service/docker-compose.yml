services:
  exiftool:
    build: .
    ports:
      - "8050:8050"
    environment:
      - EXIFTOOL_API_KEY=${EXIFTOOL_API_KEY}
      - ALLOWED_BASE=/mnt/external-photos
    volumes:
      - /mnt/external-photos:/mnt/external-photos
